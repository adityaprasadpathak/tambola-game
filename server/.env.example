# Server — environment variables
# Copy to .env and customise as needed

# Port the server listens on (default: 3001)
# PORT=3001

# Minimum number of players required before the game auto-starts (default: 6)
# Set to a lower value (e.g. 2) for local testing.
# MIN_PLAYERS=6
