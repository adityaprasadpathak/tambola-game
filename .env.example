# Frontend — Vite environment variables
# Copy to .env and customise as needed

# Optional: override the Socket.IO server URL.
# Defaults to the same host as the page (works for both localhost and LAN IPs).
# VITE_SOCKET_URL=http://192.168.1.10:3001
